# 仓库在 Windows 上开发、在 Linux（Vercel）上构建。
# 统一成 LF，避免 CRLF 在 Linux 侧引发各种诡异问题。
* text=auto eol=lf

# 二进制文件不要做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# Windows 上要保留 CRLF 的
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
